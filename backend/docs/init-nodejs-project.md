```bash
mkdir backend && cd backend

npm init -y
touch index.js .gitignore Dockerfile README.md .env

mkdir src docs
cd src

touch app.js
mkdir services configs controllers core dbs helpers models postman routes utils
```